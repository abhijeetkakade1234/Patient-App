class Auth {
    public static boolean validateLogin(String username, String password) {
        // Replace this with MongoDB authentication logic
        return "123".equals(username) && "123".equals(password);
    }
}